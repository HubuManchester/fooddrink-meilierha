<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FoodDrinkApp.Pages.SplashPage"
             BackgroundColor="#4CAF50">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <VerticalStackLayout Grid.Row="1"
                             Spacing="20"
                             HorizontalOptions="Center"
                             VerticalOptions="Center">

            <Label Text="🍽️" 
                   FontSize="80"
                   HorizontalOptions="Center"
                   TextColor="White"/>

            <Label Text="FoodDrinkApp"
                   FontSize="32"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="White"/>

            <Label Text="Track Your Favorite Foods"
                   FontSize="16"
                   HorizontalOptions="Center"
                   TextColor="#E0E0E0"/>

            <ActivityIndicator IsRunning="True"
                               Color="White"
                               HeightRequest="40"
                               Margin="0,30,0,0"/>
        </VerticalStackLayout>
    </Grid>
</ContentPage>